from setuptools import setup, find_packages

setup(
    name='desafio-intuitivecare',  # Nome do seu pacote
    version='0.1',  # Versão do pacote
    packages=find_packages(),  # Vai incluir todos os pacotes dentro do projeto
)
